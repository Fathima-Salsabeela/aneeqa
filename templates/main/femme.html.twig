{% extends 'base.html.twig' %}

{% block title %}Liste des produits
{% endblock %}

{% block body %}


   
	<h1 class="text-center my-3">Vétement pour les femme</h1>

	<div class="row">

		{% for femme in femme %}

			<div class="col-md-4">
				<div class="card mb-3" style="width: 18rem;">
					<h5 class="card-header">{{ femme.titre }}</h5> 
					<img src="{{ femme.photo }}" alt="Image du produit">
					<div class="card-body">
						<p class="card-text text-end fw-bold">{{ femme.prix / 100|number_format(2)}}€</p>
					</div>

					<div class="card-body text-end">
                    <a href="{{path("cart_add",{id:femme.id})}}" class="card-link btn btn-primary">Ajouter au panier</a>
                  </div>
				</div>
			</div>

		{% endfor %}

	</div>

{% endblock %}
